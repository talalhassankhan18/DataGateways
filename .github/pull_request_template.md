## What changed

<!-- One or two sentences. What does this do, and why now? -->

## Checklist

- [ ] `npm run lint` · `npm run typecheck` · `npm test` all pass
- [ ] No user-visible string was added to a component — copy lives in `apps/web/src/content/*.ts`
- [ ] Any new `h1`/`h2` goes through `SplitHeading`
- [ ] Any new file in `assets/` has a row in `docs/licences.csv`
- [ ] Any claim that reads as fact is either confirmed, or added to `docs/PRE-LAUNCH-SIGNOFF.md`

## Verified how

<!-- Say what you actually checked, and at which widths. Note anything you could NOT verify —
     the project log keeps an honest record of that, and it is more useful than a clean claim. -->
